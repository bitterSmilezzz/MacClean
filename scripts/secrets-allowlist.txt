# MacClean 脱敏扫描备案表（scripts/release.sh 读取）
# 格式: scope<TAB>file-or-*<TAB>rule<TAB>reason
# scope: tree=工作区跟踪文件  hist=完整 git 历史
# 私钥/GitHub/AWS/Slack/Google 这五类是绝对零命中，本表对它们**无效**，只能删。
# 每条都必须是人工核对过"确实是假数据"的，新增时写清为什么可判假。

tree	Sources/MacClean/Selftests/Selftest+ClipboardDeep.swift	private_key	剪贴板凭据检测器的输入夹具：只有一行 PEM 头、无密钥体，用于断言 checkSensitivity 会标红
hist	*	private_key	同一夹具在历史里出现过 1 次（v1.72.x 加剪贴板自检时），无密钥体
