# MacClean — Mac 原生系统清理软件

[![License: MIT](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)
[![Platform](https://img.shields.io/badge/platform-macOS_13+-lightgrey.svg)](https://developer.apple.com/macos/)
[![Swift](https://img.shields.io/badge/Swift-6.3-orange.svg)](https://www.swift.org)

> 把"让 AI 清理 Mac"的规则**固化成本地原生 App**：一键扫描、手动勾选、安全清理。
>
> **个人使用项目**：为本机定制，按需维护，欢迎参考与 fork。
> UI 风格遵循 **macOS 原生桌面设计规范**：原生源列表侧栏、inset group 分组容器、单一强调色、语义化字号阶梯、真实应用图标提取、桌面级右键菜单与原生 Toast HUD 动效。完整设计规范见 [DESIGN.md](docs/DESIGN.md)。

## 功能

- **扫描机制与安全护栏**：判定、遍历、删除三处**全链路不跟随符号链接**（`isSafeToClean` 先解析真实位置再判定；递归用 `lstat` 版 `isRealDir`；软链不计体积），`Cleaner` 的校验与删除作用于同一个已解析路径以消除 TOCTOU 缝隙；**"读不到"不再被读成"很干净"**——扫描根目录存在但不可读时会明确提示"结果不完整"并给出授权指引，而不是安静地返回 0 项。
- **单一结论，不再自相矛盾**：每个扫描项只给**一个**结论——`可清理` / `使用中` / `需确认` / `勿删`，并附一句「为什么」。
  - **不变量**：`可清理` **蕴含所属应用未在运行**。界面上永远不会再出现「可清理」和「频繁使用中」同时打在一个文件上的情况；
  - **在用检测**用三重证据取并集：所属 App 是否在运行（匹配 bundle id / 本地化名 / **CFBundleName** / 可执行名 / `.app` 目录名，覆盖"目录名英文、App 显示中文"的情况）、目录是否在 10 分钟内被写过、规则本身是否标记为需确认；
  - **"全选"只勾「可清理」项**，一键静默清理还会再要求"近期未在用"；`使用中`/`需确认` 必须由你在对应分组里显式勾选。
- **AI 再筛查与侧边问答**：
  - **逐项 AI 筛查**：扫描后一键启动 AI 二次审核，流式批量分析可清理性（可删/谨慎/不建议删）并给出明确理由；
  - **单项 ✨ 深度问答**：针对任一清理项提问——AI 判断用途、分析是否可删、检测当前占用进程（`lsof`），支持连续追问（OpenAI 兼容接口，配置本地安全存储）。
- **6 大清理分类**（对应 [CLEANUP-RULES.md](docs/CLEANUP-RULES.md) 的 52 条固化规则，规则源头见 [CleanupRules.swift](Sources/MacClean/Rules/CleanupRules.swift)）：
  用户缓存 · 日志与临时文件 · 开发残留 · App 残留 · 大文件与垃圾箱 · 浏览器与系统数据
  - **全分类原生 Quick Look 快速预览**：详情页中每个文件清理项与大文件均提供直观的「👁️ 快速查看」图标与右键菜单项，直接调用 macOS 原生 Quick Look 预览图片、音视频、文档与压缩归档，确认内容后再决定清理；
  - **应用残留与日志临时视图原生细分筛选条（v1.44.0 新增）**：
    - 应用残留视图支持按「全部残留」、「应用数据 (A1)」、「偏好设置 (A2/A5)」、「自启代理 (A3)」与「窗口状态 (A4)」快捷筛选，精准锁定遗留配置文件；
    - 日志与临时视图支持按「全部日志与临时」、「应用日志 (L1/L5)」、「崩溃与诊断 (L2/L7)」、「临时文件 (L3/L4)」与「更新残留 (L6)」细分查看；
  - **偏好碎片与系统垃圾多角度扫描增强（v1.44.0 新增）**：
    - **A4 规则**：智能排查 `~/Library/Saved Application State` 下已卸载应用的窗口恢复状态与历史会话快照；
    - **A5 规则**：深度识别 `~/Library/Preferences/ByHost` 中孤立的硬件绑定偏好碎片（自动剥离 Hardware UUID 并交叉核验在用应用）；
    - **L7 规则**：安全排查 `~/Library/Application Support/CrashReporter` 超过 30 天的历史崩溃记录与诊断日志；
  - **大文件细分类型深度解析与快捷过滤**：支持按「安装包 (dmg/pkg)」、「压缩包 (zip/tar/7z)」、「音视频 (mov/mp4/mp3)」、「RAW相机与设计原稿 (cr2/cr3/nef/arw/dng/psd/blend/c4d)」、「虚拟机与镜像 (iso/img/vdi/vmdk/parallels)」、「项目归档与开发包 (xcarchive/apk/node_modules)」、「模拟器与备份」和「其他」共 8 大细分类型精准识别，统计与批量全选自动对齐当前可见类型；
  - **大文件闲置时间区间筛选与多维排序**：支持 6 级时间跨度分档（全部 / 30天内活跃 / 1-3个月 / 3-6个月 / 半年至1年 / 1年以上闲置），并提供「推荐排序」、「体积从大到小」、「修改时间最旧优先」与「修改时间最新优先」多维排序，列表中配备闲置天数动态徽标（如 `闲置 240 天`），精准锁定常年未动的陈旧沉睡大文件；
  - **大文件与清理项清单多格式导出及外接盘迁移脚本**：扫描后支持一键导出当前分类待清理项目为标准 CSV 表格或 Markdown 文本报告。针对超大文件，更支持一键生成带有安全校验断言的 `export_move_script.sh` 外接盘 Shell 迁移脚本（`rsync -avP --remove-source-files`），插上移动硬盘即可一键安全腾挪本地空间。
- **电脑风险提醒**：独立于文件清理的敏感数据检查模块（SSH 私钥/目录权限过宽检测、明文密钥环境变量暴露排查、敏感命名文件扫描）。
- **App 卸载器与孤儿残留排查（融合 Pearcleaner / PureMac 四段式全能体系）**：
  - **已安装应用全量卸载**：提取本机已安装应用的**真实高清官方图标**，智能关联扫描 Preferences、Caches、Containers、Application Support、Logs、LaunchAgents 等全部关联数据，清晰标明每一项用途并安全移入废纸篓；
  - **「孤儿残留排查（Orphan Finder）」深度检索模式**：基于逆向反查机制，全盘遍历检索已从系统中卸载移除的遗留应用数据，涵盖沙盒容器 (`~/Library/Containers`)、共享组容器 (`~/Library/Group Containers`)、窗口恢复状态 (`Saved Application State`)、WebKit 缓存、HTTP 存储、自启代理项 (`LaunchAgents`) 及过期偏好设置 (`Preferences`)；
  - **全新「偏好碎片反查（Preference Residue Inspector v1.53.0 新增）」**：
    - 针对 `~/Library/Preferences`、`~/Library/Preferences/ByHost` 及 `~/Library/SyncedPreferences` 的专属深度逆向匹配；
    - 自动剥离 ByHost 机器硬件 UUID（正则规范匹配 36 位硬件标识符）与 `.plist` 后缀，精准还原原生 Bundle 标识；
    - 结合系统核心硬白名单（`com.apple.` 等）与 `InstalledDatabase` 在用应用/运行态多重校验，绝不误伤系统核心组件；
    - 严格执行 **7 天修改缓冲保护期**（7 天内修改的偏好配置绝不误报为孤儿），确保新装或刚启动应用稳定性；
    - 智能推导应用显示名称（包含 ShipIt 更新组件识别、常用软件库与通用格式化）；
    - 支持全选/过滤、安全清理（默认移入废纸篓，记录撤销快照）与彻底删除。
  - **全新「插件与扩展治理（Plugin & Extension Inspector v1.55.0 新增）」**：
    - **全场景扩展深度检索**：穿透扫描用户层 (`~/Library`) 与全局层 (`/Library`) 的 8 大扩展类型：QuickLook 快速查看生成器 (`.qlgenerator`)、Spotlight 元数据导入器 (`.mdimporter`)、Services 服务菜单 (`.workflow`/`.service`)、右键上下文菜单 (`.plugin`)、网页浏览器插件、屏幕保护程序 (`.saver`)、输入法与调色板组件；
    - **Bundle 结构深度解析与破损检测**：读取 `Contents/Info.plist` 提取 Bundle ID、显示名、版本与 `CFBundleExecutable`，精准诊断二进制文件丢失或死链的破损扩展（`broken`）；
    - **宿主关联与孤儿判定**：智能联动已安装应用指纹库，精确识别「宿主已卸载（孤儿）」、「扩展已损坏」、「宿主正常在用」与「macOS 系统官方内置组件」；
    - **系统缓存自愈重置**：清理 QuickLook 扩展后，后台异步自动调用 `/usr/bin/qlmanage -r` 和 `qlmanage -r cache` 重置 QuickLook 守护进程缓存，让系统即刻生效无需重启；
    - **现代化四段式原生交互**：卸载器顶栏平滑切换四段模式，配备种类筛选胶囊、健康状态分段过滤、即时搜索、一键全选可清理项、废纸篓可撤销清理与彻底删除二次确认。
  - **多重安全护栏与白名单保护**：建立 Apple 系统服务与守护进程硬白名单、正在运行进程动态防护、以及已安装应用指纹（Bundle ID / 前缀 / 归一化名称 / 可执行名）多重校验，绝不误伤系统与在用软件；支持自动按应用聚类与批量安全清理。
- **桌面级原生交互体验**：
  - **全局键盘快捷键体系**：
    - `⌃⌥Space`：**全局热键呼出/隐藏极速一键清理微面板**（支持 `⌥⌘C`、`⌃⌥C`、`⇧⌘C`、`F12` 预设，免辅助功能权限）；
    - `⌘R`：智能刷新与扫描（按当前分类/全局上下文自适应）；
    - `⌘K`：快速打开全局检索并自动对焦输入框；
    - `⌘⌫`：快速清理当前视图所选项；
    - `Space`（空格键）：快速呼出当前选中/聚焦项的 macOS Quick Look 原生预览（详情列表与重复/相似文件均支持）；
    - `⌘W` / `Esc`：优先关闭右侧 AI 助手/筛查抽屉，弹窗按 `Return` 确认或 `Esc` 取消；
    - `⌘I`：展开或收起 AI 助手；
    - `⌘,`：呼出偏好设置（AI 配置、白名单保护、巡检预警等）；
    - `⌘E`：在各分类详情页、重复文件与清理历史页快速导出 CSV / Markdown 清单；
    - `⌘1` ～ `⌘7`：全局快速直达对应清理分类。
  - **动态数字过渡（Numeric Text Transitions）**：磁盘已用、可清理总量、各分类项数与清理历史容量变化均享受系统级平滑翻页动画；
  - **微触觉按压与悬停反馈（Tactile & Hover Feedback）**：行与卡片配备按压变暗与 `macRowHover` 平滑淡入淡出插值；
  - **右键快捷菜单**：清理项与 App 残留均支持右键「在访达中高亮显示」、「拷贝绝对路径」与「切换勾选」；
  - **原生 macOS 源列表与 inset group 容器**：侧栏使用 `List(selection:)` + `.listStyle(.sidebar)`，拿回系统级高亮态、键盘导航与 vibrancy；内容区用中性底色 + 发丝分隔线的分组容器，无描边、无投影、无卡片套卡片；
  - **单一强调色**：全 App 只有一个品牌色并跟随系统强调色设置；分类身份由 SF Symbol 与文字表达，不再为 6 个分类分配 6 种颜色；
  - **系统级通知与 Dock 徽标（Badge）**：
    - **扫描完成即时通知**：分类扫描或全量扫描结束时，自动通过 macOS 原生通知中心弹出横幅提醒发现的项目数与可释放容量；
    - **清理完成通知**：安全释放完成后推送释放容量与状态报告；
    - **Dock 徽标实时同步**：随当前可清理项目总数动态增减 Dock 红底白字数字角标，清空后自动消除；
- **重复与相似大文件及相似图片排查（SHA-256 分块哈希 + dHash 感知哈希 + 智能衍生词干识别引擎）**：
  - **多级精准哈希比对**：首轮基于文件大小快速聚类，次轮读取头部 8KB 快速校验，终轮采用 `CryptoKit.SHA256` 真实 64KB 分块流哈希确认，完全一致 100% 精准无误判；
  - **相似图片感知哈希 (dHash) 智能聚类**：采用 CoreGraphics 与 ImageIO 原生底层，在解码阶段极速下采样提取 64 位差异感知哈希，通过并查集算法以汉明距离 $\le 8$（相似度 $\ge 87.5\%$）智能聚类连拍照片、轻微裁剪或不同分辨率衍生图片，行内直观展示微缩略图与相似度百分比徽章；
  - **智能相似衍生大文件识别**：自动对齐词干归一化算法（去除 ` (1)`、` copy`、`_副本`、`-backup` 等衍生后缀与跨格式文件），敏锐排查衍生副本、转码版本与同名多格式大文件；
  - **智能保留推荐与差异说明**：对完全一致副本推荐保留修改时间最早者，对相似图片优先推荐保留最高画质原图（体积大、修改时间早），对相似衍生大文件优先推荐保留高清/高容量源文件或主规范文件，并为各行标注推荐/建议原因；
  - **原生分类筛选分段器**：支持在「全部」、「完全一致」、「相似衍生」与「相似图片」间平滑切换查看，独立统计各分类浪费体积；
  - **一键智能勾选与安全清理**：一键智能勾选多余副本（保留主文件），支持批量安全移入废纸篓或彻底清理，实时统计可释放容量；
  - **原生 Quick Look 快速预览与右键交互**：支持直接点击行内眼球图标或右键快速调出系统级 Quick Look 预览图片/视频/文档内容，支持右键「在访达中显示」、「拷贝路径」与「加入白名单排除」；
  - **排查清单一键导出 (CSV / Markdown)**：支持一键导出当前重复与相似大文件排查清单，详细标注各组角色（推荐保留 / 建议清理）、文件大小、修改时间与理由，方便归档和审查。
- **网络与系统安全隐私数据深度体检（v1.52.0 重磅升级）**：
  - **系统本地 DNS 解析缓存深度体检与一键刷新 (`flushDNSCache`)**：
    - 调用原生 `dscacheutil -flushcache` 清空系统本地域名 IP 映射缓存；
    - 即刻解决陈旧域名解析污染与 CDN 回源偏慢，并彻底消除近期访问的网站域名痕迹；
  - **已记忆无线网络 (Wi-Fi) 历史轨迹排查与开放网络治理 (`NetworkPrivacyInspector`)**：
    - 自动探测 Wi-Fi 硬件接口（如 `en0`）并穿透解析全部首选无线网络列表；
    - 结合系统钥匙串认证元数据，精准识别每个 Wi-Fi 的安全加密属性（加密保护 `WPA/WPA2/WPA3` vs 开放无密码 `Open/None`）；
    - **当前活动网络强制白名单保护**：通过 `getairportnetwork` 实时锁定当前连接网络（绿标高亮），批量清理时绝对跳过，杜绝误操作断网；
    - **高危开放网络一键批量清除**：一键清除所有公共/无密码开放 Wi-Fi，防范 Evil Twin 伪热点嗅探与静默自动重连风险；
    - **单项移除与全量历史轨迹收敛**：支持单项删除特定网络或一键清空全部历史记录，收敛无线探测广播指纹（Probe Requests）；
  - **风险扫描器联动与双栏原生治理面板 (`RiskView` + `NetworkPrivacyView`)**：
    - `RiskScanner` 联动新增「未加密开放 Wi-Fi 历史记录」与「Wi-Fi 访问轨迹残留」风险规则；
    - `RiskView` 引入原生分段选择器，在「系统风险诊断」与「网络与 Wi-Fi 隐私」间无缝切换，风险项可一键直通治理。
- **菜单栏常驻助手全局快捷键呼出与极速一键清理微面板（v1.56.0 重磅新增）**：
  - **原生 Carbon Events 全局快捷键服务 (`GlobalHotkeyManager`)**：
    - 采用系统级底层 Carbon Events API 注册热键（默认 `⌃⌥Space`，支持 `⌥⌘C`、`⌃⌥C`、`⇧⌘C`、`F12` 等预设），无需申请耗时且敏感的辅助功能 (Accessibility) 权限，开箱即用；
    - 支持后台运行/全屏应用时一秒调出极速悬浮面板，失焦或 `Esc` 自动平滑隐藏；
    - 在菜单栏偏好设置中支持自定义快捷键开关与预设切换，数据自动持久化；
  - **极速悬浮微面板 (`QuickCleanPanel`)**：
    - 极简原生浮动微窗口（`NSPanel.nonactivatingPanel`），居中偏上轻巧展现；
    - 顶部整合**实时磁盘可用空间与内存占用状态仪表条**（`CapacityBar`）；
    - **一键极速释放主卡片**：智能自适应「极速释放选中项」/「一键极速推荐瘦身」/「一键安全速清」/「一键深度扫描与释放」，清理后带有即时反馈动效；
    - **快捷工具与主界面直达**：支持一键秒级「刷新 DNS 缓存（`dscacheutil`）」消除域名污染，并提供「打开主面板」快捷通道；
    - 菜单栏弹窗快捷操作区新增「极速微面板 (⌃⌥Space)」专属胶囊入口。
- **菜单栏常驻助手快捷小组件与状态指示深化（v1.51.0 重磅升级）**：
  - **底层存储快照与后台守护常驻体检微组件 (`systemToolsGroup`)**：
    - 菜单栏浮窗实时探测 APFS 本地快照与幽灵自启项（`missingExecutable` / `orphanedApp`）；
    - 提供 APFS 快照「一键释放」与幽灵自启项「一键清理」，无需开启主界面即可异步极速回收物理磁盘空间；
  - **近 7 天存储回收趋势微卡片 (`recoveryTrendGroup`)**：
    - 基于 `HistoryStore` 聚合统计近 7 天每日释放量与累计减负数据；
    - 原生 SwiftUI 渲染 7 根等宽圆角迷你趋势柱（Mini Bar Chart），动态高度映射与悬停 Tooltip 提示，可视化展现系统瘦身成效；
  - **快捷直达与浮窗尺寸微调**：
    - 菜单栏快捷直达列表扩充「启动项与后台守护」及「系统底层存储与快照」直通入口；浮窗高度调优至 530，视觉层次更加饱满紧凑。
- **空间透视（SpaceVisualizer）层级面包屑下钻导航、冷热闲置时间动态色谱与一键原位归档/外接盘迁移（v1.50.0 ~ v1.54.0 重磅升级）**：
  - **超大陈旧冷文件一键原位归档压缩与外接驱动器迁移（v1.54.0 重磅新增）**：
    - **原生高保真原位归档 (`archiveInPlace`)**：
      - 调用 macOS 底层 `/usr/bin/ditto -c -k --sequesterRsrc --keepParent` 打包为 `.zip`，完整保留 Mac 专属扩展属性（Extended Attributes）与资源分支（Resource Forks）；
      - 压缩包完整性校验，精算压缩率与释放物理空间，支持可选将原始大文件/目录安全移入系统废纸篓；
    - **外接驱动器智能探测与安全迁移 (`migrateToVolume`)**：
      - 自动枚举挂载的外部存储卷宗（过滤系统恢复卷与只读镜像），实时计算目标卷剩余容量并做前置防溢出校验；
      - 通过 `ditto` 高保真完整克隆至外接盘对应目录，校验成功后安全将本地源文件移入废纸篓，彻底解放本地 SSD；
    - **自动化迁移终端脚本生成 (`generateMigrationScript`)**：
      - 针对不便立即连盘的场景，支持一键拷贝生成带有目录存在性校验与安全断言的 `rsync -avP --remove-source-files` 终端 Shell 脚本，插上移动硬盘终端一键执行；
    - **悬停底栏与上下文菜单无缝交互**：
      - 底部悬停探查条（Detail Inspector）与右键快捷菜单动态挂载「原位归档 (.zip)」、「迁移至外接驱动器...」与「拷贝迁移脚本」入口，配备确认弹窗、外接卷选择 Sheet 与原生异步 HUD 进度提示；
  - **五级闲置冷热动态色谱 (`FileAgeLevel`)**：
    - 针对全盘大文件与深层目录，支持全新「按闲置时长」动态热力着色模式；
    - 基于文件真实修改时间（`mtime`）构建五级冷热渐变色谱：`🔥 30天内活跃`（翠绿）、`⚡ 1-3个月`（青蓝）、`🍂 3-6个月`（琥珀黄）、`🧊 半年至1年`（橙红）、`❄️ 1年以上沉睡`（紫罗兰死重冷酷色）；
    - 画布顶部自适应呈现闲置色谱图例指示条，一眼揪出常年潜伏霸占 SSD 的沉睡巨型目录与陈旧产物；
  - **深潜交互式面包屑导航条 (`BreadcrumbNavigator`)**：
    - 面包屑栏引入平滑横向滚动，无论下钻至多深的文件层级均完整平铺呈现；
    - 支持点击任意父级节点一键瞬间跨级回溯与重置至全盘根目录；
    - 当前所处物理目录右侧直通「拷贝绝对路径」（附带原生 HUD 提示）与「访达定位」原生操作；
  - **底部悬停探查条（Detail Inspector）多维情报增强**：
    - 鼠标悬浮于任一矩形树块或旭日图扇区时，动态呈现该节点的最后修改时间、闲置天数与冷热标签（如 `❄️ 闲置 412 天 (1年以上沉睡)`），并配备一键拷贝完整路径与访达定位快捷入口。
- **重复大文件并发流水线哈希与轻量指纹持久化缓存（v1.49.0 重磅升级）**：
  - **三级轻量指纹缓存持久化引擎 (`FileFingerprintCache`)**：
    - 基于文件绝对路径、文件体积与修改时间（`mtime`）构建高敏指纹校验三元组；
    - 分级缓存头 8KB 快速初筛哈希、头尾中稀疏采样哈希以及全量 SHA-256 分块哈希，支持原子化跨会话磁盘持久化（`~/Library/Caches/com.macclean.app/fingerprints.json`）；
    - **变动感知与毫秒级自愈淘汰**：文件大小或 `mtime` 一旦发生变动（浮点误差 > 1ms），旧指纹自动判定失效并淘汰，杜绝陈旧哈希误判风险；
    - **二次热扫描 0 磁盘 I/O 秒级命中**：对未修改的大文件完全跳过重复读盘，重复扫描速度提升数十倍，彻底解决数万个大文件反复哈希的磁盘与 CPU 瓶颈；
  - **并发流水线哈希调度机制 (`Pipeline Concurrent Hashing`)**：
    - 全面重构重复扫描第二阶段，将初筛校验与稀疏采样升级为 `DispatchQueue.concurrentPerform` 多核高并发流水线；
    - 动态适配系统逻辑核心数，大文件吞吐率翻倍，流水线阶段实时上报进度；
  - **底栏缓存加速效益直观洞察**：
    - 扫描完成后在底栏左侧直观呈现命中项数与节省的磁盘 I/O 读取开销（例如：`⚡ 已命中指纹缓存 84 项，节省 26.5 GB 磁盘读取`）。
- **系统底层存储深度治理：APFS 本地快照释放与休眠镜像/Swap 虚拟内存洞察（v1.48.0 重磅升级）**：
  - **APFS 本地时间机器快照透视与一键释放 (`APFSSnapshot`)**：
    - 穿透系统底层调用 `tmutil listlocalsnapshots`，排查被本地时间机器快照霸占的“幽灵物理空间”（已删除文件无法释放的根本原因）；
    - 列表展示快照名称、生成精确日期与关联宗卷，支持单项精准删除与「一键释放所有本地快照」；
  - **休眠镜像与 Swap 虚拟内存体检 (`SystemDeepStorageView`)**：
    - 深度探测 `/var/vm/sleepimage` 的物理存在与占用大小（16GB~64GB 统一内存 Mac 隐形占用大头）；
    - 统计当前 Swap 虚拟交换文件数量与总体积；
    - 读取并解析当前电源休眠模式（`hibernatemode` 0/3/25），智能结合机型（台式 Mac vs 笔记本）给出最佳省电与存储瘦身建议；
    - 提供一键复制休眠瘦身优化 Shell 脚本（包含锁定 0 字节占位防再生成机制）。
- **系统启动项与后台守护全景治理、APFS 硬链接无损去重（v1.47.0 重磅升级）**：
  - **启动项与后台守护全景体检面板 (`StartupItemManagerView`)**：
    - 全面扫描并解析 `~/Library/LaunchAgents`（用户代理）、`/Library/LaunchAgents`（全局代理）与 `/Library/LaunchDaemons`（系统守护）；
    - 深度属性解析与厂商识别：自动提取 Label、可执行文件路径、ProgramArguments、RunAtLoad、KeepAlive 等，智能归类 Apple 原生、主流软件厂商与第三方开发工具；
    - **幽灵自启残留精准诊断**：精准嗅探目标可执行文件缺失（`missingExecutable`）或宿主 App 已被彻底卸载（`orphanedApp`）的无效僵尸自启项；
    - **一键平滑停用与安全清理**：用户代理项支持一键切换禁用（`.disabled` 机制）与恢复，支持一键批量将幽灵自启配置安全移入废纸篓。
  - **重复大文件 APFS 硬链接/写时复制无损去重 (`HardlinkDedupService`)**：
    - 对内容完全一致的重复大文件，提供创新的「硬链接无损去重」：利用 APFS 底层 `link()` 机制将冗余副本替换为硬链接；
    - **保留完整文件系统路径**：不破坏任何工程依赖、文件目录结构与日常软件访问路径；
    - **秒级释放 100% 物理空间**：底层数据块即刻合并共享，瞬间夺回被重复占用的物理存储，具备严格的同宗卷与哈希一致性防护。
- **大文件音视频多维元数据解析与多维交叉透视矩阵（v1.46.0 重磅升级）**：
  - **原生 AVFoundation 深度音视频解析**：零外部依赖，毫秒级轻量提取大型媒体文件的视频分辨率（4K/1080P 等）、编码格式（HEVC/H.264/ProRes 等）、码率（Mbps/kbps）、音视频时长及音频采样率，内置高性能 LRU 解析缓存；
  - **大文件媒体胶囊徽章与摘要说明**：大文件列表中针对音视频项自动展示醒目的规格胶囊（如 `4K · 24.5 Mbps · 01:24:32`），并在副标题中呈现详细规格参数；
  - **高码率与超长时长专属排序**：大文件排序选择器新增「媒体码率最高优先」与「媒体时长最长优先」，快速揪出占用空间巨大的臃肿素材与冗余长视频；
  - **年份 × 类型多维交叉透视矩阵卡片（Cross Pivot Matrix）**：
    - **多维空间占用热点透视**：按「文件最后修改年份」与「文件类型（视频/压缩包/安装包/文稿等）」自动构建二维交叉占用矩阵，一览各年份各类文件的占用体量；
    - **Top 3 空间热点精准挖掘**：自动计算并高亮显示空间浪费最严重的 Top 3 年份类型热点（如 `2021 · 视频 45.2 GB`）；
    - **一键双向交叉下钻联动**：点击透视卡片内的任意单元格或热点胶囊，即可直接下钻应用年份与类型的复合过滤，精准收窄排查范围。
- **相似照片智能聚类与原生 EXIF 画质比对引擎（Photo Cluster & EXIF Inspector）**：
  - **原生 ImageIO EXIF 曝光参数解析**：零外部依赖深度提取相机品牌型号、镜头规格、快门曝光时间、光圈大小、ISO 感光度、等效焦距、拍摄时间与色彩空间；
  - **双栏大图对比与全屏自适应**：组内多张照片双栏并排渲染，支持悬浮局部细节比对与快捷决策；
  - **智能画质评分与保留建议**：综合考量快门防抖（更高快门优先）、控噪纯净度（更低 ISO 优先）、图像有效分辨率及文件体积，自动生成清晰保留建议（如“推荐左侧：快门 1/500s 运动抓拍更清晰，ISO 64 噪点极低”）；
  - **一键极速决策与键盘快捷盲操**：支持直接点击「保留此张 (清理对面)」或按键盘快捷键（`1` / `←` 保留左图，`2` / `→` 保留右图，`0` 均保留，`Esc` 完成退出），决策实时双向同步至主界面勾选列表与待节省空间；
  - **多图连拍组自由切换**：连拍组内超过 2 张照片时，顶部提供左右双选择器自由切换比对任意两张。
- **大文件与重复文件目录自定义树状勾选面板（Directory Tree & Scope Manager）**：
  - **原生目录树层级解析与自底向上指标聚合**：自动将扫描出的平铺大文件与重复副本解析为层级目录树结构，清晰统计每个父目录及其子目录占用的总字节数与文件数量，按体积大小自动排序；
  - **三态勾选（全选 / 全不选 / 部分勾选）**：各目录节点提供智能三态 Checkbox，支持一键批量勾选或取消该目录下所有重复副本，告别繁琐的手动单个勾选；
  - **目录分支独立视图筛选**：支持在树节点上点击「仅看此目录」，主视图即刻聚焦该特定目录分支，并于顶栏悬浮显示动态取消气泡（如 `📁 ~/Downloads/Videos ✕`）；
  - **扫描范围定制与子目录排除**：支持在目录树面板中直接调用原生 `NSOpenPanel` 添加自定义监控文件夹，或针对特定工程/隐私子目录一键设置「排除扫描」并持久化保存。
- **用户自定义白名单管理面板（防误扫误删）**：
  - **全链路排除**：支持自定义添加保护路径、文件夹、App 名称或**特定文件扩展名（如 `.iso`、`.dmg`、`.psd`、`.raw` 等）**，被保护项不仅在分类扫描与重复文件扫描中完全排除，底层 `FileSystem.isSafeToClean` 与 `Cleaner` 执行器亦层层设防，严格杜绝误删；
  - **原生偏好设置面板**：按下 `⌘,` 即可通过原生分段选择器进入「白名单保护」标签页，支持直接调用 macOS `NSOpenPanel` 原生文件夹/文件选取器，或手动输入路径与排除文件后缀（如 `iso`）；
  - **右键一键排除**：在分类详情页清理项或重复文件上右键，即可点击「加入白名单排除」或「排除所有 .xxx 格式（不再扫描）」，即刻从待选与分组中移除并持久化生效。
- **定时后台自动巡检扫描、低空间警戒与智能静默清理**：
  - **定时自动巡检**：支持在偏好设置中灵活设置巡检间隔（默认每 3 小时），后台静默定时唤醒全分类扫描并刷新磁盘健康用量；
  - **磁盘低空间预警弹窗**：实时监控当前 Macintosh HD 可用容量，当低于预设阈值（默认 15 GB，可自由调节）时，主动弹出原生警戒弹窗并推送紧急系统通知，支持「一键扫描全部分类」即刻释放空间；
  - **智能静默定时清理**：支持开启安全静默自动清理，默认配置免打扰/闲时时段（如 23:00 至次日 07:00）；遵循严格安全护栏，仅自动清理**结论为「可清理」且近期未在用**的应用缓存与过期日志，统一移入废纸篓绝不彻底删除，清理完成后静默记入流水并推送系统摘要通知。
- **系统菜单栏常驻助手与常驻调优（Menu Bar Extra v1.45.0 升级）**：
  - **休眠与唤醒感知能耗优化**：深度接入系统休眠与唤醒事件，合盖或休眠时彻底挂起定时器与后台轮询，唤醒时自动恢复并无感刷新，杜绝休眠期间无效 CPU 唤醒；
  - **菜单栏即时清理成效与撤销（Inline Clean Feedback & Undo）**：快捷清理完成后，浮窗顶部平滑展开成效横幅展示释放容量与结果摘要，并直通「撤销」动作（一键放回原位），无需进入主界面即可秒级回滚；
  - **状态栏即时概览与低空间微指示**：常驻系统右上角菜单栏，支持可配置图标显示模式（仅图标 / 图标+可用磁盘空间 / 图标+内存使用率）；磁盘跌破阈值时图标自适应提示警戒信号，且在仅图标模式下严格维持 0 内存轮询能耗不变量；
  - **活动监视器级内存压力与用量监控**：直接调用 Mach 内核底层统计，精确展示物理内存总量、已用内存、内存压力分级（正常/适中/紧张指示灯），以及 App 内存、联动内存 (Wired) 与压缩内存的细分分布；
  - **实时磁盘与可清理潜力卡片**：直观展示 Macintosh HD 实时已用、可用容量与当前可清理潜力；
  - **一键快捷行动**：无需打开庞大窗口，在菜单栏浮窗即可一键触发「智能全盘扫描」与「一键安全清理」；
  - **无缝分类直达与窗口唤醒**：在菜单栏点击各分类或重复文件、App 卸载器，即刻唤起主窗口并秒级切换到对应工作区，关闭主窗口依然保持常驻守护。
- **清理成效可视化大卡（Clean Result Sheet）**：
  - **释放成效巨幕大卡**：每次手动、聚合或快速清理完成时，自动弹出极具设计感与成就感的 macOS 原生全景大卡片；
  - **可用容量增量与前后对比**：清晰对比 Macintosh HD 清理前可用空间 vs 清理后可用空间，带动态绿色增量徽标与双色对比进度条；
  - **分类释放占比分布**：水平多色分段比例条直观展现各分类（用户缓存、开发残留、大文件等）的释放贡献比例与具体体积；
  - **一键快捷行动**：提供一键「查看废纸篓」并在访达高亮、一键直达「历史趋势」或快速确认完成。
- **清理历史与数据导出（融合 Mole `mo history` 并全面视觉增强）**：
  - **时间跨度自由筛选**：支持在「全部」、「近 7 天」与「近 30 天」间无缝切换，统计、图表与导出报告均随所选周期自适应对齐；
  - **各分类累计释放占比大卡**：自动按贡献大小排序，以各分类原生配色绘制水平堆叠比例条与多项图例，清楚洞悉空间浪费主要源头；
  - **增强释放趋势图表**：最近 14 次流水动态柱状图，实时统计平均单次释放、最高峰值与历史总计，支持悬停高亮详情气泡；
  - **一键多格式导出**：
    - **CSV 表格**：内置 UTF-8 BOM，彻底解决 Microsoft Excel 打开中文乱码，完整导出 ID、时间、分类、模式、项数、字节数等全字段；
    - **Markdown 文本报告**：自动生成包含总体统计、分类占比百分比分布与详细记录流水的高清归档报告；
    - 结合系统级 `NSSavePanel` 保存面板与快捷键 `⌘E`。
- **安全优先**：扫描只读 → 手动勾选 → 二次确认 → 默认移入废纸篓（可恢复）。
- **风险分级**：安全（可重建）/ 谨慎（需人眼确认）/ 危险（不可恢复）。
- **误删防护**：硬排除白名单（Mail/Keychains/Accounts/Messages/.ssh 等）、跳过运行中应用、bundle-id 前缀与中英文别名识别已安装 App。

功能融合来源见 [docs/FUSION-PLAN.md](docs/FUSION-PLAN.md)。

## 构建与运行

本机无 Xcode，使用 SwiftPM + CommandLineTools 构建，手工组装 .app：

```bash
# 进程内 UI 自检（ViewInspector 驱动，零窗口零打断，退出码 0=全过）
swift build
.build/debug/MacClean --selftest

# 无头扫描测试（打印本机可清理项）
.build/debug/MacClean --scan

# 打包完整 .app（包含透光晶体高清图标 + ad-hoc 签名）
./scripts/build-app.sh
open dist/MacClean.app
```

## 安装与首次打开

> 本项目为个人项目，产物为 **ad-hoc 签名（未公证）**。首次打开请先解除 Gatekeeper 隔离：

```bash
# 解压后执行（把路径换成实际位置）
xattr -cr /Applications/MacClean.app
# 然后正常打开；或右键 → 打开 → 确认
open /Applications/MacClean.app
```

**清理受 TCC 保护的位置**（Safari 数据、邮件附件等约 70% 深层垃圾）需要在「系统设置 → 隐私与安全性 → 完全磁盘访问权限」中把 MacClean 加进去，否则扫描会静默跳过这些目录（安全设计 G1）。

## AI 助手配置（可选）

侧边 AI 助手可针对任意清理项提问（用途/可否删除/是否在用）。首次使用：

1. 点 AI 面板右上 **⚙️**（或空态「去配置 AI 接口」按钮）
2. 默认已填 opencode go 网关（`https://opencode.ai/zen/go/v1` + `deepseek-v4-flash`），只需粘贴你的 API Key
   - Key **只写系统钥匙串，不落盘**；钥匙串不可用时仅保留在本次会话内存中，重启后需重新输入
   - 本 App 为 ad-hoc 签名，**重新构建会更换签名身份**导致钥匙串 ACL 失配，此时需重新粘贴一次 Key
3. 点「测试连接」验证 → 保存
4. 扫描后点任意条目旁的 ✨ 按钮提问

也支持任何 OpenAI 兼容端点（DeepSeek 官方：`https://api.deepseek.com` + `deepseek-chat`）。

> ⚠️ **隐私提示**：AI 助手会把清理项的**文件名与完整路径**发送到你配置的端点（默认是第三方网关 `https://opencode.ai/zen/go/v1`）。不希望本机文件路径离开设备时，请勿开启 AI 助手。

## 测试策略（零打断方案）

本机无 Xcode/XCTest 框架，采用 **ViewInspector 进程内自检**（`Sources/MacClean/Selftest.swift`）：

- 视图测试在**内存中驱动**（`inspect().find(...).tap()`），不渲染窗口、不抢焦点
- 按钮通过 `accessibilityIdentifier` 定位（label 含 Image 时文本查找不可靠）
- 状态用 `@Binding` 注入而非 `@State`（ViewInspector 在 macOS 上不传播 `@State` 变更）
- 覆盖 **293 项自动化自检（100% 绿灯，0 失败）**：格式化、路径展开、安全护栏、勾选逻辑、Cleaner 双模式、风险徽标、弹窗默认/切换/警告、空态禁用态、历史记录、卸载器交互、启动项治理、APFS 硬链接无损去重、底层快照与休眠镜像治理、并发流水线哈希与轻量指纹持久化缓存、空间透视层级面包屑与闲置冷热动态色谱、菜单栏快捷小组件、网络与系统安全隐私治理、偏好碎片反查、空间透视超大冷文件原位压缩归档与外接驱动器迁移、系统深度扩展与 QuickLook/Spotlight 插件残存治理、全局快捷键呼出与极速一键清理微面板等

## 目录结构

```
MacClean/
├── docs/CLEANUP-RULES.md        # 固化规则文档（与 CleanupRules.swift 同步维护）
├── scripts/build-app.sh         # .app 打包脚本
├── Resources/                   # 应用资源（透光晶体 AppIcon.icns, AppIcon.png）
├── scripts/make-icon.swift      # 备用程序化图标生成脚本
└── Sources/MacClean/
    ├── MacCleanApp.swift        # 入口（--selftest / --scan 无头模式）
    ├── Selftest.swift           # ViewInspector 进程内自检（60 项用例）
    ├── Rules/CleanupRules.swift # 规则源头：编号/分类/本质/后果说明/清理方式登记
    ├── CleanPaths.swift         # 路径规则常量 + 安全护栏清单
    ├── Scanner.swift            # 6 类扫描引擎（只读）
    ├── Cleaner.swift            # 清理执行（废纸篓/彻底删除）
    ├── FileSystem.swift         # 目录大小/枚举/安全护栏/路径归一化
    ├── AppState.swift           # 全局状态
    └── *.swift                  # Theme / Models / 现代通透视图组件
```

## 规则来源

1. 本机 agent 会话扫描（~/.agents、~/.claude、~/.dimcode 会话记录）——未发现现成"Mac 清理"技能，清理经验散见于代码治理会话与《全机安全审计报告》（Lemon 残留、Parallels keychain、rtk 钩子等卸载残留案例）；
2. 通用 macOS 清理实践（缓存/日志/DerivedData/包管理器缓存/浏览器数据/大文件）。

## 安全设计（CLEANUP-RULES.md G1–G10）

| 护栏 | 说明 |
|---|---|
| G1 | 只扫用户可写目录，权限不足跳过 |
| G2 | 手动勾选 + 二次确认 |
| G3 | 默认移入废纸篓 |
| G4 | 三档风险标记 |
| G5 | 跳过运行中应用 |
| G6 | 硬排除白名单 |
| G7 | 空目录兜底 |
